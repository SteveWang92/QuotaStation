{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "dialog",
  "description": "Allow the main window to choose a shared folder or save a diagnostic export",
  "windows": ["main"],
  "permissions": ["dialog:allow-open", "dialog:allow-save"]
}
