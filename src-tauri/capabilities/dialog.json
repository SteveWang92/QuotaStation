{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "dialog",
  "description": "Allow the main settings window to choose the shared usage folder",
  "windows": ["main"],
  "permissions": ["dialog:allow-open"]
}
